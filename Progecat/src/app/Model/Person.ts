export class Person {
  id: number;
  name: string;
  lastName: string;
  mobileNumber: string;

  constructor(id: number, name: string, lastName: string, mobileNumber: string) {
    this.id = id;
    this.name = name;
    this.lastName = lastName;
    this.mobileNumber = mobileNumber;
  }
}
